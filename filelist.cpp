#include "filelist.h"

//constructor
FileList::FileList()
{
}

//add a file to the list
void FileList::addFile()
{
    //add file to list
    //add file to widget
}

//Remove a file from the list
void FileList::removeFile()
{
    //remove the selected file from the list then shift everything below it up
    //look up how to add/remove item from the widget
}

//Move File Up the list
void FileList::MoveUp()
{
    //check if at position 0 if so do nothing
    //else swap with previous number
}

//Move File Down the list
void FileList::MoveDown()
{
    //check if at lowest position if so do nothing
    //else swap with next number

}
